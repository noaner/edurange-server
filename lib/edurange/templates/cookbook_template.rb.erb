# Users
<% users.each do |user| %>
user_account '<%= user.login %>' do
  password '<%= user.password_hash %>'
  action :create
end
<% end %>

# Recipes
<% roles.each do |role| %>
  <% role.recipes.each do |recipe| %>
    <% if recipe == 'scapy' %>
package "python-crypto"
package "python-pyx"
package "tcpdump"

# python_virtualenv "/home/ubuntu/ve" do
#   interpreter "python2.5"
#   owner "ubuntu"
#   group "ubuntu"
#   action :create
# end

python_pip "scapy" do
  action :install
  version "2.2.0-dev"
  # virtualenv "/home/ubuntu/ve"
end
    <% end %>
  <% end %>
  # Packages
  <% role.packages.each do |package| %>
package '<%= package %>'
  <% end %>
<% end %>
