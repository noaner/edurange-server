sed -i "s/PasswordAuthentication\ *no/PasswordAuthentication yes/g" /etc/ssh/sshd_config

if [ -f /etc/init.d/ssh ]
then
  service ssh reload
fi
if [ ! -f /etc/init.d/ssh ]
then
  service sshd reload
fi